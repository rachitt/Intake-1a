# ── build output & deps ───────────────────────────────────────────────────────
node_modules/
dist/
*.log

# ── assignment materials ──────────────────────────────────────────────────────
# `intake-takehome-2/` — the provided mock and specification — IS tracked, so
# the setup instructions in the README work on a fresh clone. It goes back to
# the people who issued it and nowhere else. The brief and the zip it arrived
# in are not tracked, and `takehome-1a/` is only a second extraction of the
# same archive.
*.pdf
*.zip
takehome-1a/

# ── local ─────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.env
.env.local

# End-to-end run reports and state dumps: evidence for one run, not source.
tests/results/

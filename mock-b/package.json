{
  "name": "esource-mock-b",
  "version": "1.0.0",
  "private": true,
  "description": "A second, deliberately different mock eSource platform, for testing that the agent generalizes.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
